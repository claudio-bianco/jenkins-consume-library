@Library("shared-library") _

pipeline {
    agent any
    stages {
        stage('Example') {
            steps {
                helloWorld(name:"Claudio", dayOfWeek:"Thursday")
            }
        }
    }
}
